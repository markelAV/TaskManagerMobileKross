import React, { Component } from 'react';
import {Image, View} from 'react-native';
import {Text} from 'native-base';

export default class OnboadrdingScreen extends Component {
    render() {
        return (
            <View>
                <Text style={{}}>Onboarding Screen Your ad could is here. But not now</Text>
            </View>
        );
    }
}
